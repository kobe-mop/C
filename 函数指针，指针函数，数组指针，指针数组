1.指针数组是一个数组，只不过它存放的元素都是指针 例如:int *a[10];
2.数组指针是一个指针，指向数组类型的指针  例如:int (*a)[10];

3.指针函数，此函数有返回值，返回值为int *，即返回值是指针类型的。 int *f(int a, int b); 
4.函数指针说的就是一个指针，但这个指针指向的函数，不是普通的基本数据类型或者类对象。int (*f)(int a, int b);
